import { lazy } from 'react'

const ProtectedRoutes = [
  // {
  //   path: '/dashboard',
  //   component: lazy(() => import('../../views/protected.components/dashboard/dashboard')),
  //   exact: true,
  // },
]

export default ProtectedRoutes